# This file configures Charmcraft.
# See https://juju.is/docs/sdk/charmcraft-config for guidance.

# (Required)
name: wol


# (Required)
type: charm


# (Recommended)
title: WoL server


# (Required)
summary: A Default WoL server for Daily OEM-QA Tasks.


# (Required)
description: |
  A Default WoL server for Daily OEM-QA Tasks.
  https://github.com/hanhsuan/oem-qa-tools/blob/main/Tools/env-setup/wol_server.py

# (Required for 'charm' type)
bases:
  - build-on:
    - name: ubuntu
      channel: "22.04"
    run-on:
    - name: ubuntu
      channel: "22.04"

# Add libraries
charm-libs:
  - lib: operator_libs_linux.apt
    version: "0"

config:
  options:
    # An example config option to customise the log level of the workload
    log-level:
      description: |
        Configures the log level of gunicorn.

        Acceptable values are: "info", "debug", "warning", "error" and "critical"
      default: "info"
      type: string
    port:
      description: The port that WoL server is listened
      type: int
